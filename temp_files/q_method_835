and trim rendered orientation able There rendering itself right need orient horizontal will style VERTICAL for there seems SWT window horizontally way get areas DCR tell LEFT dots handle vertical that use but drag line wanted dcr CoolBar cool vertically RIGHT bar launch this always when swt specific item intro can CoolItem Need position the left