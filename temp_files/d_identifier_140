size control right set space point move height substring area pack flush wrap exclude children fill index wraps layout bottom top cache width widgets row type swt rectangle trim string vertical get composite max last spacing wrapped extent graphics org horizontal data class rect count child compute name default hint eclipse bounds length client math left margin justify