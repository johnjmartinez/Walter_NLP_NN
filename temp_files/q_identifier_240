individually set increment int them Spinner right have really pageIncrement provide since when should spinner Slider you especially method nice selection widget pain Especially get slider setValues picky digits about like this maximum page minimum values the order