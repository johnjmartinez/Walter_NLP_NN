and all getString one Version static COM The EMPTY String quot suggest since Variant param returns version toString call has type public empty Exception return string get Always getType variant cannot cases class exception this always the com throws