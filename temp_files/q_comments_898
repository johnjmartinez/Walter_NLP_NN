specifies and code exists color seems image javadoc see threshold null incorrect information different based would opaque implementation with get when should note returns which has infer transparent return that wrong 127 not alpha data appears icon case none for fully mask public values transparency receiver the