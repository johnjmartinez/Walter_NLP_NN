and Pasting gtk rxvt exists show text into Since When gedit middle terminator using need motif null mouse string from since contains when protocol field window other has pastes xterm clipboard StyledText both This mozilla behaviour applications GTK pasting styled not end problem reproducable Motif like this button strings these length does the terminating