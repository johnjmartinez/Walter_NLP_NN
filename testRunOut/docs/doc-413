<DOC>
<DOCNO>org.eclipse.swt.browser.ProgressListener.java </DOCNO>
<text>
<class>
progress listener
</class>
<method>
 changed
 completed
</method>
<identifier>
 org
 eclipse
 swt
 browser
 org
 eclipse
 swt
 internal
swt event listener
progress listener
swt event listener
 changed
progress event
 event
 completed
progress event
 event
</identifier>
<comments>
copyright  2003  2004  ibm  corporation  and  others  all  rights  reserved  this  program  and  the  accompanying  materials  are  made  available  under  the  terms  the  eclipse  public  license  which  accompanies  this  distribution  and  available  http  www  eclipse  org  legal  epl  html  contributors  ibm  corporation  initial  api  and  implementation
this  listener  interface  may  implemented  order  receive  link progress event  notification  when  link  browser  makes  progress  loading  the  current  url  when  the  current  url  has  been  loaded  see  browser add progress listener progress listener  see  browser remove progress listener progress listener  see  browser get url  since 
this  method  called  when  progress  made  during  the  loading  the  current  location  the  following  fields  the  code progress event  code  apply  current  the  progress  for  the  location  currently  being  loaded  total  the  maximum  progress  for  the  location  currently  being  loaded  widget  the  code  browser  code  whose  current  url  being  loaded  param  event  the  code progress event  code  related  the  loading  the  current  location  code  browser  code  since 
this  method  called  when  the  current  location  has  been  completely  loaded  the  following  fields  the  code progress event  code  apply  widget  the  code  browser  code  whose  current  url  has  been  loaded  param  event  the  code progress event  code  related  the  code  browser  code  that  has  loaded  its  current  url  since 
</comments>
</text>
</DOC>
